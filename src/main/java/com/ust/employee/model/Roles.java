package com.ust.employee.model;

/**
 * This enum represents the different roles that can be assigned to a user in the system.
 * Each role has a unique name and a set of associated permissions and access levels.
 */
public enum Roles {
    ROLE_MANAGER,
    ROLE_PMO,
    ROLE_ADMIN,
    ROLE_LEADERSHIP,
    ROLE_REGULAR
}
